#!perl
use strict;
use warnings;
use Test::More tests => 1;
use Filesys::Virtual::Plain;
use Filesys::Virtual::SSH;

# A comparitive test against Filesys::Virtual::Plain, as Filesys::Virtual module is low on docs

ok(1);
